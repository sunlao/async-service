xform:
  target: dev
  outputs:
    dev:
      type: postgres
      host: aserv-postgres
      user: aserv_data
      password: "{{ env_var('DBT_PWD') }}"
      port: 5432
      dbname: db_aserv
      schema: raw
      threads: 4